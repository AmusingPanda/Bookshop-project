<% if (allrecs.length > 0) {%>

  <table border ='1'>
  <tr>
  <th>Country ID</th>
  <th>Country Name</th>
  <th>Details</th>
  <th>Edit</th>
  <th>Delete</th>
  </tr>
  <% allrecs.forEach((recref, index) => { %>
  <tr>
  <td><%= recref.country_id %></td>
  <td><%= recref.country_name %></td>
  <td><a href='/country/<%= recref.country_id %>/show'>
  Details</a></td>
  <td><a href='/country/<%= recref.country_id
  %>/edit'>Edit</a></td>
  <td><a href='/country/<%= recref.country_id %>/delete'
  onclick="return confirm('Are you sure you want to delete this record?');">Delete</a></td>
  </tr>
  <% }); %>
  </table>
  <% } else { %>
  <p>Not Records Available</p>
  <% } %>

  <p> <a href='/country/addrecord'>Add New</a> </p>
